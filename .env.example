# Database Configuration
# Set USE_POSTGRES to true to connect to PostgreSQL. Defaults to local SQLite if false or unset.
USE_POSTGRES=false

# PostgreSQL Credentials (only needed if USE_POSTGRES is true)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=retail_promotions
DB_USER=postgres
DB_PASSWORD=your_password_here
